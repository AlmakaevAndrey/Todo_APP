# Проект TODO с Vite и JSON Server

Это простой TODO-приложение с фронтендом на React + Vite и сервером на JSON Server.

## Установка

Установи зависимости:

```bash
npm install
npm run server
npm run dev
